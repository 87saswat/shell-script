#!bin/bash

# There are 4 types of commands available in Linux
# 1) Binary (/bin, /sbin) e.h. type pwd
# 2) Aliases (e.h. alias ns=netstat -tulpn will create the alias of netsat -tulpn as ns)
# 3) Shell built-in commands 
# 4) Functions